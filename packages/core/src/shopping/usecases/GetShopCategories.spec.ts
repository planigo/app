import { describe, expect, it } from '@jest/globals';
import { GetShopCategories } from "./GetShopCategories.usecase"

// describe("Get Shop categories", () => {
//     it("if there is no shop category, it should return an empty array", () => {
//         const result = []
//         //  const shopCategories = GetShopCategories.execute()
//         expect(shopCategories).toBe(result)
//     })
// })